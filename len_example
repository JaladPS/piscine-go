package main

import (
	"fmt"

	"github.com/01-edu/z01"
)

func main() {
	fmt.Print(quad("len!"), 'A')

}

func quad(q string) string {
	unicode := []rune(q)
	for a, chr := range unicode {
		for loop := 'A'; loop <= 'A'; loop++ {

			z01.PrintRune(chr)
			fmt.Println(a)
		}
	}
	return ""
}
